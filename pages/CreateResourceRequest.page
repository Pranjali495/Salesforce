<apex:page standardController="Opportunity" extensions="CreateResourceRequestController" action="{!validateAndUpdate}" >

    <apex:pageMessages />
    
    <apex:form >
    	<apex:outputPanel layout="block" >
        	<apex:commandButton action="{!proceedIfNotWon}" value="Create" rendered="{!showCreate}" />
            <apex:commandButton action="{!cancel}" value="Cancel" />
    	</apex:outputPanel>
    </apex:form>
    
</apex:page>